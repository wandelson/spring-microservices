server:
  port: ${PORT:8761}
  servlet:
    context-path: /eureka
    
    
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL1:http://8761/eureka/eureka}
    register-with-eureka: false
    fetch-registry: false
  instance:
    ip-address: true
  server:
    enable-self-preservation: true
    wait-time-in-ms-when-sync-empty: 3000
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    beans:
      enabled: true
    health:
      show-details: ALWAYS
  info:
    build:
      enabled: true
      
      